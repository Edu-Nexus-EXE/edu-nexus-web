---
description: Inspect existing repo patterns and relevant official docs before implementing
allowed-tools: Read, Glob, Grep, Bash(rg:*)
---

Research before coding for: $ARGUMENTS

Steps:

1. Search local files with `rg --files` and targeted `rg`.
2. Read nearby Edu Nexus examples before proposing new abstractions.
3. Check `AGENTS.md` and `ARCHITECTURE.md` for placement and constraints.
4. If external docs are needed, prefer official docs and clearly cite what was inferred.
5. Recommend reuse, wrap, or build with a short rationale.

Do not install packages during this command.
